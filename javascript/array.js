/**
 * A basic implementation of a dynamic array data structure in JS
 * 
 * Methods to implement: access, push, pop, delete 
 */

class MyArray {
    constructor() {
        this.length = 0;
        this.data = {};
    }
}